# Copy this file to `local.properties` and set sdk.dir to your Android SDK path.
# Android Studio creates local.properties automatically when you open the project,
# so this is mostly a hint for first-time-from-CLI users.
#
# Example (macOS):
#   sdk.dir=/Users/yourname/Library/Android/sdk
